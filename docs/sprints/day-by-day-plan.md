# Day-by-day plan: 2026-07-21 → 2026-07-29

Companion to `sprint-01.md` and `sprint-02.md` — this is the concrete, per-person schedule for the remainder of the MVP window, covering the tail of Sprint 01 through the final capstone presentation. See `docs/prd/Riser-PRD.md` for the requirements this plan builds toward, and `docs/architecture/integration-contracts.md` for the exact API contracts and external-API integration details referenced below.

## Context

Two things changed the shape of this plan versus what the sprint files originally assumed:

1. **The real deadline is tighter than `sprint-02.md`'s stated range.** That file says Sprint 02 runs through 2026-08-01, but the actual final capstone presentation is **Wednesday 2026-07-29** — three days earlier. The Saturday 2026-07-25 week-1 demo lines up exactly with Sprint 01's documented end date, so that part is unaffected; Sprint 02 compresses into four days (Sun–Wed) instead of seven. **Update 2026-07-24: the Saturday 7/25 Week 1 demo is confirmed cancelled** — it is not happening. The only remaining presentation deadline is the final capstone on Wednesday 7/29. See the Sat 7/25 entry below for what that changes.
2. **Sprint 01's P0 core is already done, not "TBD."** The per-person checklists in `sprint-01.md`/`sprint-02.md` were still blank placeholders as of 2026-07-21, and `git log` on `apps/compliance`/`frontend/src` shows only two commits (`feat(backend): scaffold Django 6 + DRF compliance API`, `feat(frontend): scaffold React 19 + TypeScript + Vite SPA`), which reads like scaffolding. A status check confirmed the substance is actually there: Building/Elevator CRUD, the due-date/status service (CAT1 +1yr / CAT5 +5yr, Compliant/Warning≤30d/Delinquent), the sorted ledger endpoint, 34 backend tests at **100% coverage** on `apps/compliance`, and a working, tested React ledger UI (`LedgerPage`, `StatusBadge`, `BuildingForm`/`ElevatorForm`, API client). The checklists just were never checked off. This means the open work from here is P1 polish plus the two stretch items from the PRD v2 draft, not the P0 core itself.
3. **The team has a full work session tonight, 2026-07-21**, ahead of the originally-assumed 7/22 start. That's one extra day of runway ahead of the final capstone — used below as a second buffer/dry-run day (Fri 7/24) rather than compressing the existing task list, since a live demo benefits more from rehearsal slack than from finishing early.

Two decisions shape the back half of this plan:

- **AI agent (issue #1):** build the **proactive risk-narration briefing** (Option 3) — an on-demand, plain-language summary of ledger state generated by calling the Claude API over already-correct, already-computed data. Chosen over the chat-copilot option for lower live-demo risk (no multi-turn unpredictability to rehearse).
- **Priority for the rest of the sprint:** the AI agent is treated as **non-negotiable** (likely a capstone grading requirement per issue #1's own framing). The DOB Open Data auto-populate stretch (address-lookup, per the PRD v2 draft, using the free NYC Open Data "DOB NOW: Elevator Safety Compliance" dataset, resource `e5aq-a4j2`) was originally scoped **best-effort** on the assumption its riskiest unknown (geocoding reachability) was unresolved — see below, that's no longer the case, so it now stays on the Mon/Tue schedule rather than being the first thing cut.

**Revision, 2026-07-25 (Sat) — both stretch features elevated from "best-effort" to committed.** A fresh-eyes evaluation of the PRD against the actual repo state on 2026-07-25 (see `docs/team/status-log.md` for that date) found: (1) the DOB address-lookup feature isn't just a nice-to-have — it's the thing the PRD's own §1a revision note stakes the project's "sharper, more defensible" opportunity claim on ("the gap is synthesis and triage, not data collection"); without it, the shipped MVP solves a narrower problem than the one the PRD argues for, by the PRD's own admission. (2) The PRD's requirements table already marks both the narration briefing (line 127) and the core DOB address-lookup bullet (line 87) 🔴 P0 — this plan's own cut-order previously contradicted that by treating DOB lookup as the first/third thing to cut. That inconsistency is resolved below: **both features are now committed for Sun–Tue, not best-effort.** Zero code exists for either as of Sat 7/25 evening (confirmed: no `narration.py`, no `/api/buildings/lookup/` endpoint, no frontend panel or lookup form) — Stages 1-2 of the DOB pipeline (`dob.py`) and the frontend TS types/client methods for both (PR #43) are the only pieces already done. That leaves real, non-trivial work across exactly **three working days** (Sun 7/26, Mon 7/27, Tue 7/28 — Wed 7/29 is presentation-morning-only).

Given last week's attendance pattern (Cornell/Andres/Schiffon each missed multiple consecutive sessions; nearly every Wed-Fri task slipped 1-4 days before Karl picked it up), the days below keep the **same role assignments as before** per Karl's standing offer to keep covering gaps, but change _how fast_ the fallback triggers: instead of discovering a slip 2-4 days later, each day now has an explicit same-day checkpoint (midday and end-of-day). If the assigned owner hasn't pushed visible progress (a branch or commit, not just "started") by a checkpoint, Karl (with Claude Code) starts building that piece in parallel immediately, not after the day closes out. If the original owner shows up later and is ahead, their work lands instead; this isn't a reassignment, just insurance against another multi-day gap with only three days of runway left.

Property managers enter a building by **address**, not BIN — BIN resolution happens on the backend, invisibly, address in, BIN/BBL out, via NYC Planning's GeoSearch v2. Reflected in Mon/Tue below.

**Reachability risk flagged during the contracts spec (`docs/architecture/integration-contracts.md`) — resolved 2026-07-22.** The address→BIN resolver this plan originally assumed (NYC Planning's GeoSearch **v1**) returned HTTP 410 Gone on a live check. Cornell's research slot (done two days ahead of its Friday 7/24 schedule, alongside a working POC) confirmed **GeoSearch v2 is the live replacement, keyless, and returns the BIN directly** — no Geoservice API key needed. Full findings: `docs/architecture/geocoding-reachability-findings.md`; working resolver/disambiguation/fetch code: `backend/apps/compliance/dob.py` (merged). This de-risks Monday's integration work substantially — Stage 1 (address→BIN) and Stage 2 (BIN→devices) of the pipeline in `integration-contracts.md` §4 are already built and tested; what's left is the device→Elevator mapping decision, the DRF endpoint composing all three stages, and the frontend form.

Role assignments follow the split the team agreed to in issue #2 (Karl's proposal, accepted): **Cornell → backend/services**, **Andres → frontend logic/data-fetching**, **Schiffon (Lola) → visual/interaction/a11y**, **Karl → API contract seam + integration**, **docs-sync → rotates to whoever has slack that day**.

## Coordination notes (avoiding file conflicts)

Checked this schedule for days where two people's tasks land in the same file. Three patterns need explicit sequencing — issue #2 already established the principle ("sequence rather than parallelize on shared components"); these are where it applies here:

- **Wed 7/22 — `LedgerPage.tsx`/`StatusBadge.tsx`:** Andres (filter/group-by-building, building name inline) and Schiffon (status-change highlight animation) both touch ledger-rendering code the same day. **Andres lands his change first (morning), pushes, then Schiffon branches from that and layers the animation on top (afternoon)** — same pattern as the original Sprint 01/02 split. Also: Andres's filter UI needs `LedgerListView` to actually support `?building=` filtering, which it currently does **not** (only `ElevatorViewSet` does — see `docs/architecture/integration-contracts.md` §2). **Cornell should land that filter early in the day**, or Andres builds the dropdown UI shell against the already-working `elevators/?building=` endpoint first and swaps to the ledger endpoint once Cornell's piece lands.
- **Sun 7/26 → Tue 7/28 — the AI-narration panel and the address-lookup screens:** in each case Andres builds the frontend shell/logic and Schiffon styles/polishes the same component, on the _same day_, three days running (Sun: narration panel; Mon: narration edge cases + address-lookup form shell; Tue: address-lookup wiring + review screen). **Same rule each day: Andres's piece lands and is pushed before Schiffon starts on that file.** Worth a 5-minute handoff check-in each morning rather than assuming it'll naturally work out.
- **Sun 7/26 & Mon 7/27 — Karl's "define the contract" tasks:** these are **frontend-type-and-client-only plus spec alignment, not backend edits** — the exact request/response shapes are already pre-agreed in `docs/architecture/integration-contracts.md` (written ahead of time, not co-designed live), so Karl only touches `frontend/src/types/domain.ts` and `frontend/src/api/client.ts` while Cornell is independently writing the actual endpoint in `views.py`/`urls.py`/a new service module. No same-file overlap as long as both build against the spec doc rather than negotiating shape in code together.
- **Wed 7/22 — `backend/pyproject.toml`/`.env.example`:** Karl's environment-prep task touches files in Cornell's usual lane. Low risk (one-line dependency addition, unlikely to collide with anything Cornell's doing that day), but Karl should give Cornell a heads-up before touching it.
- **Tue 7/21 tonight — low risk, no action needed:** Andres (`ElevatorForm.tsx`) and Schiffon (new empty-state component, likely mounted in `LedgerPage.tsx`) touch different files; Andres's live-update check is verification, not a lasting edit. Flagged only for completeness.
- **Thu 7/23 & Fri 7/24 (testing/buffer days):** by nature these days involve fixing whatever testing turns up, which can land anyone in any file. General rule for both days: whoever finds a bug fixes it if it's in their own lane; if it's in someone else's file, flag it to that person rather than patching it yourself, and commit/push in small increments so fixes don't collide.

## Housekeeping to fold in on day 1 (not a separate task later)

- Update `docs/sprints/sprint-01.md` and `docs/sprints/sprint-02.md` per-person checklists to reflect (a) that Sprint 01's P0 items are actually done, and (b) the real Sprint 02 scope/dates (compressed to 7/26–7/29, AI agent + DOB stretch added). This closes out issue #2's remaining checklist item.
- ~~Decide whether the v2 PRD replaces the canonical PRD~~ — done: `docs/prd/Riser-PRD.md` (and its `.docx` submission copy) now _is_ the v2 content, on this branch/PR. No decision left to make here.

## Day-by-day

### Tue 2026-07-21 (tonight) — Kickoff, verify P0, close small gaps, start P1

- **Whole team (first 30 min):** confirm Sprint 01 P0 is solid — rerun `uv run pytest --cov` (backend) and `npm run test:coverage` (frontend), walk through the ledger UI together end-to-end. Do the shared TDD/red-green-refactor walkthrough now if it hasn't happened yet, per Karl's issue #2 comment, so nobody hits the coverage gate by surprise later.
- **Karl:** update both sprint files' checklists per Housekeeping; walk the team through the new contracts spec (`docs/architecture/integration-contracts.md`) so everyone's building against the same shapes from day one.
- **Cornell:** confirm the API supports editing an existing elevator's date/type (`PATCH` on `ElevatorViewSet`); add it with tests if it's missing.
- **Andres:** wire the elevator-edit UI to that endpoint (confirm `ElevatorForm` already supports an edit mode; extend it if not) and confirm due-date/status/rank update live on save.
- **Schiffon:** build the polished empty-state screen (P1) for the zero-devices first-run case.

### Wed 2026-07-22 — P1 polish continues

- **Cornell:** add building-scoped filtering support (query param) to `LedgerListView` specifically (`ElevatorViewSet` already has it) — land this early in the day so Andres isn't blocked; with tests.
- **Andres (morning):** implement filter/group-by-building in the ledger UI against Cornell's param; show building name inline on each ledger row. Push when done.
- **Schiffon (afternoon, after Andres pushes):** build the status-change highlight/animation on top of Andres's updated `LedgerPage.tsx`/`StatusBadge.tsx`; visual QA pass on the new empty state.
- **Karl:** contract review on the day's two new pieces (edit shape, filter param) — confirm TS types match DRF serializers exactly. Separately: add the `anthropic` SDK to `backend/pyproject.toml` and an `ANTHROPIC_API_KEY` placeholder to `backend/.env.example`/`.env` — pure environment prep for Sunday's AI agent work, no feature code yet.

### Thu 2026-07-23 — Integration pass, dry-run #1

- **Whole team:** full regression pass — rerun both test suites, confirm the 90% coverage gate still holds after this week's changes, fix anything broken.
- **Cornell:** sweep backend edge cases that are cheap now and expensive to hit live at the final demo (leap-year due-date math, boundary-day Warning/Delinquent transitions).
- **Andres:** check the ledger/forms under realistic portfolio size (25+ elevators, per the PRD's own success metric) and on the browser the demo will actually run in.
- **Schiffon:** final visual pass; confirm status colors meet the "distinct, high-contrast" requirement.
- **Karl:** write the demo script/talking points framed around the PRD's Problem/Opportunity section; run a full dry-run with the team in the afternoon.

### Fri 2026-07-24 — Buffer day, dry-run #2

The extra day tonight's session bought us. Use it to actually fix what dry-run #1 exposed, not to start new scope.

- **Whole team:** triage and fix everything dry-run #1 turned up; re-run both test suites and the coverage gate once more after fixes land.
- **Cornell:** done early (2026-07-22) — confirmed GeoSearch v2 is reachable/keyless and built a working, tested address→BIN→DOB-device resolver POC (`backend/apps/compliance/dob.py`, `docs/architecture/geocoding-reachability-findings.md`), well ahead of this research-only slot. Nothing left to do here; if there's spare time today, sketch the remaining device→Elevator mapping question (a device can carry both a CAT1 and CAT5 date — see `integration-contracts.md` §4 Stage 3) ahead of Monday.
- **Andres:** final cross-browser/responsive check on whatever machine will actually run the eventual demo.
- **Schiffon:** final accessibility pass (contrast, jsx-a11y, axe) across everything shipped this week.
- **Karl:** run dry-run #2 with the whole team in the afternoon. Scope freeze was originally tied to Saturday's demo; with that cancelled (see Context above), there's no hard freeze point today — but don't use the freed-up time to open new scope beyond what's already planned Sun–Tue below without a deliberate call to do so.

### Sat 2026-07-25 — no demo (cancelled)

**Confirmed 2026-07-24: the Week 1 demo originally planned for today is not happening.** No presentation, no forced scope freeze. Treat today as open buffer time ahead of Sunday's AI-agent work — worth using for whatever of Thu/Fri's still-outstanding manual items (`docs/team/status-log.md` — Andres's/Schiffon's checks) can get picked up, rather than losing the day to a rehearsal that no longer has an audience. The retro originally planned for "end of day" is optional; the Sun–Tue plan below stands regardless.

**Add-on, later same day: ad hoc Pursuit curriculum "Polish Session" (1:30pm–3:45pm), not on this plan.** Separate from the buffer-time work above — a scheduled Pursuit module requiring crash-safety/error-handling (plus logging), a UI-navigability pass, and README problem/solution documentation, done today specifically to satisfy that curriculum checkpoint rather than because the day-by-day plan called for it. See `docs/team/status-log.md`'s same-day entry for what shipped and PR #74.

### Sun 2026-07-26 — AI agent: backend, contract, and full UI (committed, not just "backend + contract")

Narration is the simpler of the two remaining features (one endpoint, one panel) and is the one already marked non-negotiable, so it's front-loaded to land **completely** today, freeing Mon/Tue entirely for DOB lookup.

- **Cornell:** build the narration-briefing service — takes the already-computed ledger state and makes a single-turn Claude API call to produce a plain-language summary of urgent/recent state (e.g., "3 elevators are Delinquent, 2 enter Warning this week — prioritize X first"); new read-only endpoint (`GET /api/ledger/narration/`) per `docs/architecture/integration-contracts.md` §5. Test-first with the Anthropic client mocked — assert prompt construction and response handling, not live model output. **Checkpoint: a branch/PR with the service function (even pre-endpoint) should exist by midday.**
- **Karl:** TS type + client method already shipped early (PR #43, merged 7/25) — nothing left here except a fresh-eyes check that `fetchNarration()`/`NarrationResponse`/`NarrationErrorResponse` still match §5 once Cornell's endpoint is live. **Fallback trigger: if Cornell has no branch by midday, Karl builds `narration.py` + the endpoint himself immediately** (spec is fully written in §5 — no design decision needed, straight implementation), so Andres isn't blocked into the evening.
- **Andres (once the endpoint exists, from either Cornell or the Karl fallback):** build the panel component that calls the endpoint on demand and renders the narration, with a loading state. Push when done. **Checkpoint: panel should be up and calling the real endpoint by end of day. Fallback: if Andres hasn't pushed by then, Karl builds the panel** (Schiffon's styling task the same day depends on it existing, so this can't slip to Monday without also delaying DOB-lookup work).
- **Schiffon (after Andres pushes, same day if possible):** design where the panel lives on the ledger page and its empty/loading/error states, layered onto Andres's component — it should read as one integrated feature, not a bolted-on chat box. If Schiffon isn't present, this becomes Monday morning's first task (Karl fallback) rather than slipping silently — functional narration without final polish is still demo-safe.
- **End of day:** narration should be demoable end-to-end (button → real Claude call → rendered summary), even if Schiffon's polish pass hasn't landed yet.

**Status update, 2026-07-26 (Sun) early afternoon:** Andres's panel is merged (PR #78) — built as a functional shell against the documented §5 response shape rather than waiting on a live endpoint, same pattern Monday's plan already allows for the address-lookup form. Karl pre-wrote the narration endpoint's test suite, `xfail`-marked, ahead of Cornell's implementation (PR #77). Cornell's narration service/endpoint itself hasn't started as of this update, and the midday checkpoint above has technically passed — but Cornell is present and actively working on it this session, so the Karl-fallback trigger is intentionally **not** invoked yet, per the standing rule of not preempting a same-day task while its owner is actively on it (see `docs/team/status-log.md`'s 2026-07-26 entry). Also landed today, outside this day's original scope: ADR-0002 confirmed with the team and issue #76 closed (PR #82); Andres's README screenshots reviewed and merged (PR #81); a schedule gap around the capstone demo script/deck was identified and a Mon 7/27 evening slot added to close it (PR #83, open) — see the Mon 7/27 section below.

### Mon 2026-07-27 — DOB integration: backend pipeline + form shell

**Scope note (updated 2026-07-24, still accurate):** thanks to Cornell's early POC, Stage 1 (address→BIN) and Stage 2 (BIN→devices) of the DOB pipeline are already built and tested. Today's work: the device→Elevator mapping (Stage 3), the composing endpoint, and the frontend form shell — the full two remaining working days (today + Tue) are now dedicated to this one feature.

- **Cornell:** build `map_dob_devices_to_drafts()` (Stage 3 — one `ElevatorDraft` per populated cat1/cat5 date field, per the resolved mapping decision in `integration-contracts.md` §4 Stage 3), then the `/api/buildings/lookup/` endpoint composing `resolve_address()` → `fetch_devices()` → `map_dob_devices_to_drafts()`. Exact request/response shapes: §3. To keep scope tight, **skip `Elevator.data_source` for now** (flagged optional/P2 in §3's "New model fields" — a nullable field with no migration risk to add post-demo); do add `Building.bin` and `Elevator.dob_device_number` since the review/override flow needs them. **Checkpoint: `map_dob_devices_to_drafts()` merged by midday; endpoint skeleton (even without full error-reason branching) pushed by end of day.**
- **Karl:** the address-lookup TS type/client method already shipped early (PR #43) — verify it matches §3 once Cornell's endpoint is live. **Fallback trigger: if Cornell has no Stage-3/endpoint branch by midday, Karl starts the endpoint himself** (composing already-built Stage 1/2 functions is mechanical; the only real design decision — the mapping rule — is already resolved in the doc).
- **Andres (morning, can start against the _documented_ response shape without waiting for the live endpoint):** build the address-lookup form shell (a single address field — no BIN exposed to the user), mocked against the §3 response shape. Swap to the real endpoint once it's live. Push when done. **Checkpoint: form shell pushed by end of day regardless of backend status** — it doesn't need to block on Cornell/Karl's piece to start.
- **Schiffon (afternoon, after Andres pushes):** start visual design for the "review and override before saving" screen on top of Andres's form shell. If Schiffon isn't present, this slips to Tuesday's polish slot rather than blocking Andres/Cornell's critical-path work.
- **Schiffon (evening, reassigned 2026-07-27 from Karl — was a schedule gap added 2026-07-26):** rewrite `docs/demo/week-1-demo-script.md` into the real capstone script — same hook/close structure, but extended to cover AI narration and DOB address-lookup (both landing this Sun–Tue) and reframed for the Wed 7/29 capstone audience instead of the cancelled Week 1 demo. Start the slide deck (no deck exists yet anywhere in the repo). Doing this tonight, not Tuesday, so Tuesday's dress rehearsal is rehearsing an actual script/deck instead of building them live under time pressure.

### Tue 2026-07-28 — DOB integration finish + full rehearsal

- **Cornell:** finish the DOB service — no-match/error/upstream-unavailable fallback to manual entry (all three already spec'd as 200-with-`reason` responses in `integration-contracts.md` §3), tests. **Checkpoint: fallback branching merged by midday** so Andres has the full response contract to wire the review flow against for the rest of the day.
- **Andres (morning):** wire the address-lookup form to the endpoint; build the review/override-before-saving flow (list pulled devices, let the user edit/uncheck before it POSTs through the existing `createBuilding`/`createElevator` calls per §3's "read-only/preview" design). Push when done. **Fallback trigger: if Andres hasn't pushed a working wired form by early afternoon, Karl takes over the review/override flow** — this is the last checkpoint before rehearsal, so there's no slack left to wait past it.
- **Schiffon (afternoon, after Andres pushes):** polish the review/override screen; fold in any leftover narration-panel polish from Sunday if that didn't land.
- **Karl:** end-to-end contract verification through the morning. **No cut call by default** — both features are committed per the 7/25 revision above. If, and only if, by mid-afternoon the DOB flow still doesn't work end-to-end despite the fallback triggers above, fall back to demoing it as a backend-only proof (curl/Postman against `/api/buildings/lookup/` showing real DOB data) rather than a live broken UI flow — narration is not subject to this fallback; it must be live-UI by today regardless, since it landed Sunday.
- **Whole team, afternoon/evening:** full dress rehearsal for Wednesday — PRD north star → live ledger → AI narration → DOB auto-populate.

### Wed 2026-07-29 — Final capstone presentation

- Morning: last-look rehearsal, fix only true blockers.
- Present.

## Cut order if the team falls behind (most expendable first)

**Revised 2026-07-25:** DOB address-lookup no longer sits in this list as a standing cuttable item — see the 2026-07-25 revision note above. Both it and narration are 🔴 P0 in the PRD's own requirements table (lines 87, 127), and DOB lookup specifically is what the PRD's §1a opportunity claim depends on; treating it as routinely cuttable while narration was "never cut" was an inconsistency between this plan and the PRD, not a real priority difference. The Tue 7/28 section above still names a narrow, explicit fallback (backend-only proof instead of a live UI flow) if the same-day checkpoint triggers don't recover it in time — that's a last-resort demo-format fallback, not a standing item on this list.

1. Remaining P2 items (delete building/elevator, status-only filter, sample/demo data button).
2. Filter/group-by-building (P1) — real, but least tied to the AI-agent grading risk.
3. **Never cut:** the P0 ledger core, the AI narration agent, DOB address-lookup (per the 2026-07-25 revision), and rehearsal time before the final capstone presentation (the only presentation deadline left, since the Sat 7/25 Week 1 demo is cancelled).

## Post-MVP backlog (not scheduled this sprint)

- **Filing-summary / proof-of-service generation** (PRD Journey 3, clarified 2026-07-22 per Andres): pre-filled filing-summary PDF for the manager to hand/email to their licensed inspection agency, plus recordkeeping of the resulting proof-of-service against the device. Real P3 value, proposed as a possible swap for the DOB integration slot — not adopted as a swap (see above), but worth picking up post-capstone. See `docs/prd/Riser-PRD.md` §3 and `docs/design/mental-model-briefing.md` §6.
- **Auth / read-write vs. read-only enforcement** (confirmed out of scope for the MVP demo 2026-07-26 — see `docs/adr/0002-no-auth-for-mvp.md`, issue #76): the PRD's property-manager (read/write) vs. building-owner (read-only) distinction, currently unenforced. Nice-to-have if the team gets ahead of schedule before 7/29; otherwise post-capstone.

## Verification

Before the final capstone presentation (the only demo day left on the calendar), run the same checklist:

- `backend/`: `uv run pytest --cov --cov-fail-under=90` and `uv run ruff check .`
- `frontend/`: `npm run test:coverage`, `npm run typecheck`, `npm run build`
- Manual golden-path walk: add a building manually, add one via address lookup (once built), edit an elevator's inspection date and watch due-date/status/rank update live, trigger the AI narration panel and confirm it reads sensibly against current ledger data.

## Key files

- Backend core: `backend/apps/compliance/{models,services,serializers,views,urls}.py`, `backend/apps/compliance/tests/`
- Frontend core: `frontend/src/features/ledger/LedgerPage.tsx`, `frontend/src/features/portfolio/{BuildingForm,ElevatorForm}.tsx`, `frontend/src/components/StatusBadge.tsx`, `frontend/src/api/client.ts`
- New AI-agent work: a new service module in `backend/apps/compliance/` (e.g. `narration.py`) + endpoint in `views.py`/`urls.py`; new frontend panel component alongside `LedgerPage.tsx`
- New DOB-integration work: a new backend service module for address→BIN resolution + the Socrata client; new frontend address-lookup form alongside the existing portfolio forms
- Docs to update day 1: `docs/sprints/sprint-01.md`, `docs/sprints/sprint-02.md` (per-person checklists still need real task fill-in)
- Full contract shapes and external-API integration details: `docs/architecture/integration-contracts.md`
- Env/config: `backend/pyproject.toml` (add `anthropic` dependency), `backend/.env.example`/`.env` (add `ANTHROPIC_API_KEY`)
