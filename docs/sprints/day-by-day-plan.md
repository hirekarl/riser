# Day-by-day plan: 2026-07-21 → 2026-07-29

Companion to `sprint-01.md` and `sprint-02.md` — this is the concrete, per-person schedule for the remainder of the MVP window, covering the tail of Sprint 01 through the final capstone presentation. See `docs/prd/Riser-PRD.md` for the requirements this plan builds toward, and `docs/architecture/integration-contracts.md` for the exact API contracts and external-API integration details referenced below.

## Context

Two things changed the shape of this plan versus what the sprint files originally assumed:

1. **The real deadline is tighter than `sprint-02.md`'s stated range.** That file says Sprint 02 runs through 2026-08-01, but the actual final capstone presentation is **Wednesday 2026-07-29** — three days earlier. The Saturday 2026-07-25 week-1 demo lines up exactly with Sprint 01's documented end date, so that part is unaffected; Sprint 02 compresses into four days (Sun–Wed) instead of seven.
2. **Sprint 01's P0 core is already done, not "TBD."** The per-person checklists in `sprint-01.md`/`sprint-02.md` were still blank placeholders as of 2026-07-21, and `git log` on `apps/compliance`/`frontend/src` shows only two commits (`feat(backend): scaffold Django 6 + DRF compliance API`, `feat(frontend): scaffold React 19 + TypeScript + Vite SPA`), which reads like scaffolding. A status check confirmed the substance is actually there: Building/Elevator CRUD, the due-date/status service (CAT1 +1yr / CAT5 +5yr, Compliant/Warning≤30d/Delinquent), the sorted ledger endpoint, 34 backend tests at **100% coverage** on `apps/compliance`, and a working, tested React ledger UI (`LedgerPage`, `StatusBadge`, `BuildingForm`/`ElevatorForm`, API client). The checklists just were never checked off. This means the open work from here is P1 polish plus the two stretch items from the PRD v2 draft, not the P0 core itself.
3. **The team has a full work session tonight, 2026-07-21**, ahead of the originally-assumed 7/22 start. That's one extra day of runway before Saturday's demo — used below as a second buffer/dry-run day (Fri 7/24) rather than compressing the existing task list, since a live demo benefits more from rehearsal slack than from finishing early.

Two decisions shape the back half of this plan:

- **AI agent (issue #1):** build the **proactive risk-narration briefing** (Option 3) — an on-demand, plain-language summary of ledger state generated by calling the Claude API over already-correct, already-computed data. Chosen over the chat-copilot option for lower live-demo risk (no multi-turn unpredictability to rehearse).
- **Priority after Saturday's demo:** the AI agent is treated as **non-negotiable** (likely a capstone grading requirement per issue #1's own framing); the DOB Open Data auto-populate stretch (address-lookup, per the PRD v2 draft, using the free NYC Open Data "DOB NOW: Elevator Safety Compliance" dataset, resource `e5aq-a4j2`) is **best-effort** and the first thing cut if the team falls behind.

Property managers enter a building by **address**, not BIN — BIN resolution happens on the backend, invisibly, address in, BIN/BBL out, via a to-be-confirmed NYC Planning geocoding service (see the reachability caveat below). Reflected in Mon/Tue below.

**Risk flagged during the contracts spec (`docs/architecture/integration-contracts.md`):** the address→BIN resolver this plan assumes (NYC Planning's GeoSearch API, assumed free/no-key) returned HTTP 410 Gone on a live check, while NYC Planning's current replacement (Geoservice API) requires a registered API key. Cornell's Friday research slot below must resolve this before Monday's integration work starts; if neither option is quickly usable, the address-lookup feature falls back to manual BIN entry or gets cut per the cut-order below.

Role assignments follow the split the team agreed to in issue #2 (Karl's proposal, accepted): **Cornell → backend/services**, **Andres → frontend logic/data-fetching**, **Schiffon (Lola) → visual/interaction/a11y**, **Karl → API contract seam + integration**, **docs-sync → rotates to whoever has slack that day**.

## Coordination notes (avoiding file conflicts)

Checked this schedule for days where two people's tasks land in the same file. Three patterns need explicit sequencing — issue #2 already established the principle ("sequence rather than parallelize on shared components"); these are where it applies here:

- **Wed 7/22 — `LedgerPage.tsx`/`StatusBadge.tsx`:** Andres (filter/group-by-building, building name inline) and Schiffon (status-change highlight animation) both touch ledger-rendering code the same day. **Andres lands his change first (morning), pushes, then Schiffon branches from that and layers the animation on top (afternoon)** — same pattern as the original Sprint 01/02 split. Also: Andres's filter UI needs `LedgerListView` to actually support `?building=` filtering, which it currently does **not** (only `ElevatorViewSet` does — see `docs/architecture/integration-contracts.md` §2). **Cornell should land that filter early in the day**, or Andres builds the dropdown UI shell against the already-working `elevators/?building=` endpoint first and swaps to the ledger endpoint once Cornell's piece lands.
- **Sun 7/26 → Tue 7/28 — the AI-narration panel and the address-lookup screens:** in each case Andres builds the frontend shell/logic and Schiffon styles/polishes the same component, on the _same day_, three days running (Sun: narration panel; Mon: narration edge cases + address-lookup form shell; Tue: address-lookup wiring + review screen). **Same rule each day: Andres's piece lands and is pushed before Schiffon starts on that file.** Worth a 5-minute handoff check-in each morning rather than assuming it'll naturally work out.
- **Sun 7/26 & Mon 7/27 — Karl's "define the contract" tasks:** these are **frontend-type-and-client-only plus spec alignment, not backend edits** — the exact request/response shapes are already pre-agreed in `docs/architecture/integration-contracts.md` (written ahead of time, not co-designed live), so Karl only touches `frontend/src/types/domain.ts` and `frontend/src/api/client.ts` while Cornell is independently writing the actual endpoint in `views.py`/`urls.py`/a new service module. No same-file overlap as long as both build against the spec doc rather than negotiating shape in code together.
- **Wed 7/22 — `backend/pyproject.toml`/`.env.example`:** Karl's environment-prep task touches files in Cornell's usual lane. Low risk (one-line dependency addition, unlikely to collide with anything Cornell's doing that day), but Karl should give Cornell a heads-up before touching it.
- **Tue 7/21 tonight — low risk, no action needed:** Andres (`ElevatorForm.tsx`) and Schiffon (new empty-state component, likely mounted in `LedgerPage.tsx`) touch different files; Andres's live-update check is verification, not a lasting edit. Flagged only for completeness.
- **Thu 7/23 & Fri 7/24 (testing/buffer days):** by nature these days involve fixing whatever testing turns up, which can land anyone in any file. General rule for both days: whoever finds a bug fixes it if it's in their own lane; if it's in someone else's file, flag it to that person rather than patching it yourself, and commit/push in small increments so fixes don't collide.

## Housekeeping to fold in on day 1 (not a separate task later)

- Update `docs/sprints/sprint-01.md` and `docs/sprints/sprint-02.md` per-person checklists to reflect (a) that Sprint 01's P0 items are actually done, and (b) the real Sprint 02 scope/dates (compressed to 7/26–7/29, AI agent + DOB stretch added). This closes out issue #2's remaining checklist item.
- ~~Decide whether the v2 PRD replaces the canonical PRD~~ — done: `docs/prd/Riser-PRD.md` (and its `.docx` submission copy) now _is_ the v2 content, on this branch/PR. No decision left to make here.

## Day-by-day

### Tue 2026-07-21 (tonight) — Kickoff, verify P0, close small gaps, start P1

- **Whole team (first 30 min):** confirm Sprint 01 P0 is solid — rerun `uv run pytest --cov` (backend) and `npm run test:coverage` (frontend), walk through the ledger UI together end-to-end. Do the shared TDD/red-green-refactor walkthrough now if it hasn't happened yet, per Karl's issue #2 comment, so nobody hits the coverage gate by surprise later.
- **Karl:** update both sprint files' checklists per Housekeeping; walk the team through the new contracts spec (`docs/architecture/integration-contracts.md`) so everyone's building against the same shapes from day one.
- **Cornell:** confirm the API supports editing an existing elevator's date/type (`PATCH` on `ElevatorViewSet`); add it with tests if it's missing.
- **Andres:** wire the elevator-edit UI to that endpoint (confirm `ElevatorForm` already supports an edit mode; extend it if not) and confirm due-date/status/rank update live on save.
- **Schiffon:** build the polished empty-state screen (P1) for the zero-devices first-run case.

### Wed 2026-07-22 — P1 polish continues

- **Cornell:** add building-scoped filtering support (query param) to `LedgerListView` specifically (`ElevatorViewSet` already has it) — land this early in the day so Andres isn't blocked; with tests.
- **Andres (morning):** implement filter/group-by-building in the ledger UI against Cornell's param; show building name inline on each ledger row. Push when done.
- **Schiffon (afternoon, after Andres pushes):** build the status-change highlight/animation on top of Andres's updated `LedgerPage.tsx`/`StatusBadge.tsx`; visual QA pass on the new empty state.
- **Karl:** contract review on the day's two new pieces (edit shape, filter param) — confirm TS types match DRF serializers exactly. Separately: add the `anthropic` SDK to `backend/pyproject.toml` and an `ANTHROPIC_API_KEY` placeholder to `backend/.env.example`/`.env` — pure environment prep for Sunday's AI agent work, no feature code yet.

### Thu 2026-07-23 — Integration pass, dry-run #1

- **Whole team:** full regression pass — rerun both test suites, confirm the 90% coverage gate still holds after this week's changes, fix anything broken.
- **Cornell:** sweep backend edge cases that are cheap now and expensive to hit live Saturday (leap-year due-date math, boundary-day Warning/Delinquent transitions).
- **Andres:** check the ledger/forms under realistic portfolio size (25+ elevators, per the PRD's own success metric) and on the browser the demo will actually run in.
- **Schiffon:** final visual pass; confirm status colors meet the "distinct, high-contrast" requirement.
- **Karl:** write the demo script/talking points framed around the PRD's Problem/Opportunity section; run a full dry-run with the team in the afternoon.

### Fri 2026-07-24 — Buffer day, dry-run #2

The extra day tonight's session bought us. Use it to actually fix what dry-run #1 exposed, not to start new scope.

- **Whole team:** triage and fix everything dry-run #1 turned up; re-run both test suites and the coverage gate once more after fixes land.
- **Cornell:** if P0/P1 are solid with time to spare, spend remaining time confirming which NYC Planning geocoding service is actually reachable (GeoSearch vs. Geoservice API — see the risk flagged above and in `docs/architecture/integration-contracts.md`) and reviewing the DOB Open Data (`e5aq-a4j2`) response shape — research only, no code — to de-risk Monday's integration work rather than starting it early.
- **Andres:** final cross-browser/responsive check on whatever machine will actually run the demo.
- **Schiffon:** final accessibility pass (contrast, jsx-a11y, axe) across everything shipped this week.
- **Karl:** run dry-run #2 with the whole team in the afternoon; freeze scope for Saturday — nothing new lands after this.

### Sat 2026-07-25 — Week 1 demo

- Morning: last rehearsal, fix only what the rehearsal exposes — no new scope.
- Demo: P0 core + whatever P1 polish landed, told through the PRD's actual problem framing (fragmented manual tracking → one ranked, live-updating view).
- End of day (whole team, brief): retro on actual pace this week to sanity-check the Sun–Tue plan below before committing to it.

### Sun 2026-07-26 — AI agent: backend + contract

- **Cornell:** build the narration-briefing service — takes the already-computed ledger state and makes a single-turn Claude API call to produce a plain-language summary of urgent/recent state (e.g., "3 elevators are Delinquent, 2 enter Warning this week — prioritize X first"); new read-only endpoint per the pre-agreed contract in `docs/architecture/integration-contracts.md`. Test-first with the Anthropic client mocked — assert prompt construction and response handling, not live model output.
- **Karl:** add the matching TS type (`domain.ts`) and API client method (`client.ts`) per the spec doc — frontend-only, doesn't touch Cornell's backend files. Keep it strictly on-demand/button-triggered, not push — consistent with the "no automated notifications" framing.
- **Andres (once Karl's client method exists):** build the panel component that calls the endpoint on demand and renders the narration, with a loading state. Push when done.
- **Schiffon (after Andres pushes):** design where the panel lives on the ledger page and its empty/loading/error states, layered onto Andres's component — it should read as one integrated feature, not a bolted-on chat box.

### Mon 2026-07-27 — AI agent polish + DOB integration kickoff

- **Cornell:** finish AI-agent tests/coverage; then start the DOB Open Data client service — resolve the entered **address** to a BIN via whichever NYC Planning geocoding service Friday's research confirmed reachable, then `GET https://data.cityofnewyork.us/resource/e5aq-a4j2.json?bin=...`, mapping `device_number`/`cat1_latest_report_filed`/`cat5_latest_report_filed`/`device_status` fields onto new Building/Elevator records. Exact request/response shapes: `docs/architecture/integration-contracts.md`.
- **Karl:** the "add building by address" contract is already pre-defined in `docs/architecture/integration-contracts.md` — add its TS type/client method (frontend-only, same pattern as Sunday); do a fresh-eyes end-to-end review of yesterday's AI-agent contract.
- **Andres (morning):** close out AI-panel edge cases (empty portfolio, API error state); start the address-lookup form shell (a single address field — no BIN exposed to the user). Push when done.
- **Schiffon (afternoon, after Andres pushes):** AI-panel accessibility/visual QA pass; start visual design for the "review and override before saving" screen on top of Andres's form shell.

### Tue 2026-07-28 — DOB integration finish (best-effort) + full rehearsal

- **Cornell:** finish the DOB service — no-match/error fallback to manual entry, tests.
- **Andres (morning):** wire the address-lookup form to the endpoint; build the review/override-before-saving flow. Push when done.
- **Schiffon (afternoon, after Andres pushes):** polish the review/override screen.
- **Karl:** end-to-end contract verification; **by midday, make the cut call** — if DOB integration isn't solid, demo it as a backend-only proof (or drop it) rather than risk a shaky live flow. This is the point where the "best-effort" priority decision gets enforced.
- **Whole team, afternoon:** full dress rehearsal for Wednesday — PRD north star → live ledger → AI narration → (if it survived the cut call) DOB auto-populate.

### Wed 2026-07-29 — Final capstone presentation

- Morning: last-look rehearsal, fix only true blockers.
- Present.

## Cut order if the team falls behind (most expendable first)

1. DOB Open Data address auto-populate (explicitly agreed best-effort).
2. Remaining P2 items (delete building/elevator, status-only filter, sample/demo data button).
3. Filter/group-by-building (P1) — real, but least tied to the AI-agent grading risk.
4. **Never cut:** the P0 ledger core, the AI narration agent (per the team's explicit priority call), and rehearsal time before either demo.

## Verification

Before each demo day, run the same checklist:

- `backend/`: `uv run pytest --cov --cov-fail-under=90` and `uv run ruff check .`
- `frontend/`: `npm run test:coverage`, `npm run typecheck`, `npm run build`
- Manual golden-path walk: add a building manually, add one via address lookup (once built), edit an elevator's inspection date and watch due-date/status/rank update live, trigger the AI narration panel and confirm it reads sensibly against current ledger data.

## Key files

- Backend core: `backend/apps/compliance/{models,services,serializers,views,urls}.py`, `backend/apps/compliance/tests/`
- Frontend core: `frontend/src/features/ledger/LedgerPage.tsx`, `frontend/src/features/portfolio/{BuildingForm,ElevatorForm}.tsx`, `frontend/src/components/StatusBadge.tsx`, `frontend/src/api/client.ts`
- New AI-agent work: a new service module in `backend/apps/compliance/` (e.g. `narration.py`) + endpoint in `views.py`/`urls.py`; new frontend panel component alongside `LedgerPage.tsx`
- New DOB-integration work: a new backend service module for address→BIN resolution + the Socrata client; new frontend address-lookup form alongside the existing portfolio forms
- Docs to update day 1: `docs/sprints/sprint-01.md`, `docs/sprints/sprint-02.md` (per-person checklists still need real task fill-in)
- Full contract shapes and external-API integration details: `docs/architecture/integration-contracts.md`
- Env/config: `backend/pyproject.toml` (add `anthropic` dependency), `backend/.env.example`/`.env` (add `ANTHROPIC_API_KEY`)
